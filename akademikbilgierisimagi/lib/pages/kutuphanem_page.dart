import 'package:flutter/material.dart';

class KutuphanemPage extends StatelessWidget {
  const KutuphanemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Kütüphanem',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
